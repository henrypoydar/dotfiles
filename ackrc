# Run `ack --dump` to see the defaults that this 
# config complements

# Always color, even if piping to another program
--color

# More directories to ignore
--ignore-dir=cache
--ignore-dir=gems

# Add common filetypes
--type-add=markdown:ext:md,mdown,markdown
--type-add=coffeescript:ext:coffee
