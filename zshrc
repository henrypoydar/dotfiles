# Slow prompt? Uncomment this line and the `zprof` line below to see what's holding things up
# zmodload zsh/zprof

. ~/.zsh/aliases.zsh
. ~/.zsh/asdf.zsh
. ~/.zsh/bindings.zsh
. ~/.zsh/config.zsh
. ~/.zsh/docker.zsh
. ~/.zsh/completions.zsh
. ~/.zsh/fasd.zsh
. ~/.zsh/fzf.zsh
. ~/.zsh/history.zsh
. ~/.zsh/java.zsh
. ~/.zsh/prompt.zsh
. ~/.zsh/vimify.zsh
. ~/.zsh/yarn.zsh

# zprof
